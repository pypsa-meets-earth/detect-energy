import torch
import torch.nn as nn

class Xception(nn.Module):
    """
    Object recognition model to estimate probability of AC in input image
    """
    
    def __init__(self):
        super(Xception, self)
        
        raise NotImplementedError("implement me!")
        

if __name__ == "__main__":
    model = Xception()